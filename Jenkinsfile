pipeline {
    agent any
    options {
        timestamps()
    }
    stages {

        stage('代码构建') {
            steps {
                sh "cd dmx-user-center"
              
            }
        }

        stage('运行程序') {
            steps {
                echo "run programer"
            }
        }

    }
}
